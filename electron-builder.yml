appId: com.example.nextron
productName: My Nextron App
copyright: Copyright © 2018 Yoshihide Shiono
directories:
  output: dist
  buildResources: resources
win:
  artifactName: "myNextronApp.${version}.${ext}"
mac:
  artifactName: "myNextronApp.${version}.${ext}"
linux:
  artifactName: "myNextronApp.${version}.${ext}"
files:
  - from: .
    filter:
      - package.json
      - app
publish:
  - provider: github
    owner: herisusantoarismann
    repo: nextron-auto-update
